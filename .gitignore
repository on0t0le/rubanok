# pipeline binary
pipeline/pipeline
pipeline-bin

# SQLite database (ephemeral, created during pipeline run)
pipeline.db

# pipeline output (published to gh-pages branch, not committed to main)
output/
